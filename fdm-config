#The software global variables are defined here

VERSION=beta-2

#Choose between text and ncurse UI
#If ncurse ui is choosen but the dialog libs are not available, the text UI will be used
#UI="ncurses"
#UI="text"
UI="text"

#User directories
CONFDIR="$HOME/.config/fdm"
CACHEDIR="${CONFDIR}/cache"

#X session storage
X=${CONFDIR}/X

#Wayland session storage
WAYLAND=${CONFDIR}/Wayland

#Other session storage (tty, etc)
EXTRA=${CONFDIR}/extra

#Default session link
DEFAULT=${CONFDIR}/default

#Save the latest used session as the default
#at the exception of the extra sessions
SAVELAST=1

#Auto exec default UI without selection
AUTOSTART=0