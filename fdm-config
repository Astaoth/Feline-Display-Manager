#The software global variables are defined here

VERSION=0.3.2

#Choose between text and ncurse UI
#If ncurse ui is choosen but the dialog libs are not available, the text UI will be used
#UI="ncurses"
#UI="text"
UI="text"

#User directories
CONFDIR="$HOME/.config/fdm"
CACHEDIR="${CONFDIR}/cache"

#X session storage
X=${CONFDIR}/X

#Other session storage (weston, tty, etc)
EXTRA=${CONFDIR}/extra

#Default session
DEFAULT=${CONFDIR}/default

#Save the latest used session as the default
SAVELAST=1
